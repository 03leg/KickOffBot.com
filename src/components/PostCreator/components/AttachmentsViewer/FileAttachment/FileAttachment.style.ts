import { makeStyles } from "tss-react/mui";
import { Colors } from "~/themes/Colors";

export const useFileAttachmentStyles = makeStyles()(() => ({
  action: {
    backgroundColor: Colors.WHITE,
  },
}));
