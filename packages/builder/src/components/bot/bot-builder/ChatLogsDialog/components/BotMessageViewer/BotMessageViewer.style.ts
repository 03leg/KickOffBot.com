import { makeStyles } from "tss-react/mui";

export const useBotMessageViewerStyles = makeStyles()(
  ({ spacing, palette }) => ({
    root: {
      color: "#3F51B5",
      fontStyle: "italic",
    },
  })
);
