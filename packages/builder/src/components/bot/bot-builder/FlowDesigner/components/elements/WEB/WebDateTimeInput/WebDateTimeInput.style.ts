import { makeStyles } from "tss-react/mui";

export const useWebDateTimeInputStyles = makeStyles()(() => ({
  root: {
    display: "flex",
    flexDirection: "column",
    alignItems: "flex-start",
  },
}));
