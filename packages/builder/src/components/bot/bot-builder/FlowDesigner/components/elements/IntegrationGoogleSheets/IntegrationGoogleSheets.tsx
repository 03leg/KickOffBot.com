import { GoogleSheetsIntegrationUIElement } from '@kickoffbot.com/types';
import React from 'react';

interface Props {
    element: GoogleSheetsIntegrationUIElement;
}

export const IntegrationGoogleSheets = ({ element }: Props) => {
    return (
        <div>Configure &apos;Google spreadsheets&apos;...</div>
    )
}
