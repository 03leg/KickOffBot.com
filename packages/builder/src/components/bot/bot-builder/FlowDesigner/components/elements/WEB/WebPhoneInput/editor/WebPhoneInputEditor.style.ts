import { makeStyles } from "tss-react/mui";

export const useWebPhoneInputEditorStyles = makeStyles()(({ spacing }) => ({
  editorTitle: {
    marginTop: spacing(1),
  },
  variableSelector: {
    marginTop: spacing(1),
  },
}));
