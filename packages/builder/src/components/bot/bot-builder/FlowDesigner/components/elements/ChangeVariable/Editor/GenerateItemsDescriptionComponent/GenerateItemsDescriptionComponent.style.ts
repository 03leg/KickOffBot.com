import { makeStyles } from "tss-react/mui";

export const useGenerateItemsDescriptionComponentStyles = makeStyles()(({ spacing }) => ({
  root: {
    display: "flex",
    flexDirection: "column",
  },
}));
